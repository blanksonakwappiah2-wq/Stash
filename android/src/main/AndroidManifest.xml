<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".QuickBiteApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickBite"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- Login Activity -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickBite.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.QuickBite.NoActionBar" />

        <!-- Main Activity (Customer) -->
        <activity
            android:name=".ui.customer.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.QuickBite.NoActionBar" />

        <!-- Restaurant Detail Activity -->
        <activity
            android:name=".ui.customer.RestaurantDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.QuickBite.NoActionBar" />

        <!-- Order Tracking Activity -->
        <activity
            android:name=".ui.customer.OrderTrackingActivity"
            android:exported="false"
            android:theme="@style/Theme.QuickBite.NoActionBar" />

        <!-- Owner Dashboard Activity -->
        <activity
            android:name=".ui.owner.OwnerDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.QuickBite.NoActionBar" />

        <!-- Delivery Agent Activity -->
        <activity
            android:name=".ui.agent.DeliveryAgentActivity"
            android:exported="false"
            android:theme="@style/Theme.QuickBite.NoActionBar" />

    </application>

</manifest>
