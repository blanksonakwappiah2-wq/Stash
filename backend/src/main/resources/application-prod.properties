# Production Profile

# Server Port (Render injects PORT env var)
server.port=${PORT:8080}

# Data Source - PostgreSQL (Render provides connection string via environment variable)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/quickbite}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Actuator (Monitoring)
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always

# Caching
spring.cache.type=simple

# Disable H2 console (H2 is on classpath but must not run in prod)
spring.h2.console.enabled=false
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.h2.H2ConsoleAutoConfiguration

# Google Maps API Key
google.maps.api.key=${GOOGLE_MAPS_API_KEY:}

# JWT Configuration
jwt.secret=${JWT_SECRET:quickbite-secret-key-for-jwt-token-generation-must-be-long-enough-for-security}
jwt.expiration=${JWT_EXPIRATION:86400000}
